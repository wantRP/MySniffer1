<Window x:Class="MySniffer1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MySniffer1"
				xmlns:viewmodel="clr-namespace:MySniffer1.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Window.Resources>
		<viewmodel:ViewModel x:Key="vm"></viewmodel:ViewModel>
	</Window.Resources>
	<Grid  DataContext="{Binding Source={StaticResource vm}}">
		<Grid.RowDefinitions>
			<RowDefinition Height="20"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal">
			<ComboBox Name="comboBoxInterfaces" Grid.Row="0" Width="200"
								ItemsSource="{Binding Path=InterfaceList}"  
								SelectedItem="{Binding Path=Interface,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
								SelectionChanged="comboBoxInterfaces_SelectionChanged">
				<ComboBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding InterfaceName}"/>

					</DataTemplate>
				</ComboBox.ItemTemplate>
			</ComboBox>
			<Button Command="{Binding Path=IStartSniffing}">Start</Button>
		</StackPanel>
		<Button Content="Button" HorizontalAlignment="Left" Margin="165,180,0,0" Grid.Row="1" VerticalAlignment="Top"
						Command=""/>

	</Grid>
</Window>
